import { getDate, isToday } from "date-fns";

export function loadToday() {
    
}

export function addToday() {
    
}

//const result = isToday(new Date(2014, 9, 6, 14, 0)) //pt aflare in ce zi suntem
//=> true


// sau : getDate();

const date = newDate();console.log(date);